/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DynamicComponent2Directive } from './dynamic-component2.directive';

describe('Directive: DynamicComponent2', () => {
  it('should create an instance', () => {
    const directive = new DynamicComponent2Directive();
    expect(directive).toBeTruthy();
  });
});
